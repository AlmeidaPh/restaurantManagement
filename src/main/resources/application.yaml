spring:
  application:
    name: restaurant

  security:
    user:
      name: user
      password: password

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/restaurant_db}
    username: ${DATABASE_USER:restaurant_admin}
    password: ${DATABASE_PASSWORD:restaurant_21102007}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  devtools:
    restart:
      enabled: true